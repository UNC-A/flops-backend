version: "3.8"  # Specify the Docker Compose version
networks:
  flops:

services:
  db:
    image: mongo
    ports:
      - "${DB_PORT}:${DB_PORT}"
    volumes:
      - ./db:/data
    env_file:
      - .env
    restart: always
    networks:
      - flops


  ws:
    build: .
    ports:
      - "${PORT}:${PORT}"
    command: ["cargo", "run", "-r"]  # run for release
    #command: ["cargo", "run", "-r"]  # run for debug
    env_file:
      - .env
    depends_on:
      - db
    restart: always
    networks:
      - flops